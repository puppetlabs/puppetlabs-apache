<% Array(@additional_includes).each do |include| -%>
  
  ## Load additional static includes
  <IfVersion < 2.4>
    Include "<%= include %>"
  </IfVersion>
  <IfVersion >= 2.4>
    IncludeOptional "<%= include %>"
  </IfVersion>
<% end -%>
