<Location <%= @status_path %>>
    SetHandler server-status
<%= scope.call_function('template', ["apache/mod/_require.erb"]) -%>
</Location>
ExtendedStatus <%= @extended_status %>

<IfModule mod_proxy.c>
    # Show Proxy LoadBalancer status in mod_status
    ProxyStatus On
</IfModule>
